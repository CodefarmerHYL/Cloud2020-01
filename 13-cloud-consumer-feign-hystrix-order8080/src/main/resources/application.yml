server:
  port: 8080

spring:
  application:
    name: hystrix-feign-consumer-order #\u5165\u4F4F\u8FDB EurekaServer \u7684\u540D\u79F0

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka

#feign:
#  hystrix:
#    enabled: true
